<section class="card">
    <div class="container">
    <img class="card-img" src="{{photoURL}}" />
    </div>
    <div class="description">
	{{description}} 
    </div>
    <div class="tags">
        Tags:
    {{#each tags}}
	  {{this}}
	{{/each}}
    </div>
</section>
